# Copy to .env and fill in. .env is gitignored and must stay that way.

# Used to call every agent in the pool during reward collection.
OPENROUTER_API_KEY=

# Optional: only needed for gated upstream datasets (e.g. GPQA) and for publishing.
HF_TOKEN=
